[build]
builder = "NIXPACKS"
buildCommand = "pip install -r api-orders/requirements.txt"

[deploy]
startCommand = "sh -c 'cd api-orders && alembic upgrade head; uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8003}'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
