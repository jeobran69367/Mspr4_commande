[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd api-orders && alembic upgrade head || true && cd api-orders && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8003}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
