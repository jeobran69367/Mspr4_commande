[build]
builder = "DOCKERFILE"
dockerfilePath = "api-orders/Dockerfile"

[deploy]
startCommand = "bash entrypoint.sh"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
healthcheckPath = "/health"
healthcheckTimeout = 100
