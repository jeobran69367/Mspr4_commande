[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "cd api-orders && alembic upgrade head && RUN_MIGRATIONS=false uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8003}"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
